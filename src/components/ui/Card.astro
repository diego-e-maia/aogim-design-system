---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`rounded-xl border border-[rgb(var(--color-border))] bg-[rgb(var(--color-bg))] overflow-hidden ${className}`} data-reveal="up">
  <slot />
</div>
